# based on https://d-i.debian.org/manual/example-preseed.txt

# https://tinyurl.com/preseedlaia
# Enable debugging
d-i debian-installer/quiet false
d-i debian-installer/splash false

### Localization
d-i debian-installer/language string en
d-i debian-installer/country string DE
d-i debian-installer/locale select en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us

# netcfg will choose an interface that has link if possible. This makes it
# skip displaying a list if there is more than one interface
d-i netcfg/choose_interface select auto

# If you have a slow dhcp server and the installer times out waiting for
# it, this might be useful.
d-i netcfg/dhcp_timeout string 60
d-i netcfg/dhcpv6_timeout string 60

# Automatic network configuration is the default
# d-i netcfg/disable_autoconfig boolean true

# Host
d-i netcfg/get_hostname string lgrobe42
d-i netcfg/get_domain string 42berlin.de

# Disable that annoying WEP key dialog.
d-i netcfg/wireless_wep string

### Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string debian.netcologne.de
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Do not install a desktop environment
tasksel tasksel/first multiselect standard system utilities, SSH server

### Account setup
# Skip creation of a root account (normal user account will be able to
# use sudo).
#d-i passwd/root-login boolean false
# Alternatively, to skip creation of a normal user account.
#d-i passwd/make-user boolean false

# Root password, either in clear text
d-i passwd/root-password password helloworld
d-i passwd/root-password-again password helloworld

# To create a normal user account.
d-i passwd/user-fullname string Laia Grobe
d-i passwd/username string laia
# Normal user's password, either in clear text
d-i passwd/user-password password helloworld
d-i passwd/user-password-again password helloworld
# Create the first user with the specified UID instead of the default.
#d-i passwd/user-uid string 1010

### Clock and time zone setup
# Controls whether or not the hardware clock is set to UTC.
d-i clock-setup/utc boolean true

# You may set this to any valid setting for $TZ; see the contents of
# /usr/share/zoneinfo/ for valid values.
d-i time/zone string Europe/Berlin
# Controls whether to use NTP to set the clock during the install
d-i clock-setup/ntp boolean true

# Base Partitioning (MBR style)
### Partitioning
d-i partman-auto/disk string /dev/sda

d-i partman-auto/expert_recipe string \
    bonus-partitioning :: \
        # /boot partition (Primary)
        500 500 500 ext2 \
            $primary{ } $bootable{ } \
            method{ format } format{ } \
            use_filesystem{ } filesystem{ ext2 } \
            mountpoint{ /boot } \
        . \
        # Extended partition (to contain the logical crypto partition)
        33000 33000 33000 $lvmok{ } \
            $extended{ } $lvmignore{ } \
        . \
        # Encrypted partition (Logical) - this will be the LVM Physical Volume
        33000 33000 33000 $lvmignore{ } \
            $lvmok{ } $defaultignore{ } \
            method{ crypto } \
            format{ true } \
            pv{ yes } \
            $logical{ } \
        . \
        # Logical Volume: root (on encrypted LVM)
        10000 10000 10000 ext4 \
            lv_name{ root } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ / } \
        . \
        # Logical Volume: swap (on encrypted LVM)
        2300 2300 2300 linux-swap \
            lv_name{ swap } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ swap } \
        . \
        # Logical Volume: home (on encrypted LVM)
        5000 5000 5000 ext4 \
            lv_name{ home } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ /home } \
        . \
        # Logical Volume: var (on encrypted LVM)
        3000 3000 3000 ext4 \
            lv_name{ var } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ /var } \
        . \
        # Logical Volume: srv (on encrypted LVM)
        3000 3000 3000 ext4 \
            lv_name{ srv } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ /srv } \
        . \
        # Logical Volume: tmp (on encrypted LVM)
        3000 3000 3000 ext4 \
            lv_name{ tmp } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ /tmp } \
        . \
        # Logical Volume: var/log (on encrypted LVM)
        4000 4000 4000 ext4 \
            lv_name{ var--log } vg_name{ LVMgroup } in_vg{ LVMgroup } \
            mountpoint{ /var/log } \
        .

# Select the custom recipe
d-i partman-auto/expert_recipe_name string bonus-partitioning
d-i partman-auto/choose_recipe select bonus-partitioning


# Encryption Passphrase
d-i partman-crypto/passphrase password helloworld
d-i partman-crypto/passphrase-again password helloworld
d-i partman-crypto/confirm_nooverwrite boolean true
d-i partman-crypto/confirm boolean true

# LVM Confirmation
d-i partman-lvm/confirm boolean true
d-i partman-lvm/confirm_nooverwrite boolean true

# Confirmation
d-i partman/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

### Apt setup
# Choose, if you want to scan additional installation media
# (default: false).
d-i apt-setup/cdrom/set-first boolean false

# This is fairly safe to set, it makes grub install automatically to the UEFI
# partition/boot record if no other operating system is detected on the machine.
d-i grub-installer/only_debian boolean true

# This one makes grub-installer install to the UEFI partition/boot record, if
# it also finds some other OS, which is less safe as it might not be able to
# boot that other OS.https://d-i.debian.org/manual/example-preseed.txt
d-i grub-installer/with_other_os boolean true


d-i finish-install/reboot_in_progress note
d-i cdrom-detect/eject boolean false
